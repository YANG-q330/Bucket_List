{% extends 'base.html.twig' %}

{% block title %}{{ wish.titre }}|{{ parent() }}{% endblock %}

{% block body %}
    <div class="container">
        <h2>{{ wish.titre }}</h2>
        <div>
            <p>{{ wish.description }}</p>
            <p>Author : {{ wish.author }}</p>
            <p>Published at : {{ wish.dateCreated|date("d/m/Y à H:i") }}</p>
        </div>

    </div>
{% endblock %}

